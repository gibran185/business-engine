$kind: http-request
name: Services Controller create
url: "{{baseUrl}}/merchants/:merchantId/services"
method: POST
headers:
  Content-Type: application/json
pathVariables:
  - key: merchantId
    value: "{{merchantId}}"
    $type: |-
      {
        "type": "object",
        "properties": {
          "value": {
            "type": "string"
          }
        },
        "required": [
          "value"
        ]
      }
body:
  type: json
  content: |-
    {
      "name": "Haircut for man",
      "description": "Standard cut and style for man",
      "price": 175,
      "durationMinutes": 45,
      "isActive": true
    }
  $type: |-
    {
      "type": "object",
      "properties": {}
    }
auth:
  type: bearer
  credentials:
    token: "{{bearerToken}}"
examples: ./.resources/Services Controller create.resources/examples
order: 1000
