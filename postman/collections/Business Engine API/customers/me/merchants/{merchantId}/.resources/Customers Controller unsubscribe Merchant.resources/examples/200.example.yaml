$kind: http-example
request:
  url: "{{baseUrl}}/customers/me/merchants/:merchantId"
  method: PATCH
  headers:
    Content-Type: application/json
  pathVariables:
    merchantId: string
  body:
    type: json
    content: "{}"
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
