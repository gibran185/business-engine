$kind: http-example
request:
  url: "{{baseUrl}}/merchants/config/:slug"
  method: GET
  pathVariables:
    slug: string
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
