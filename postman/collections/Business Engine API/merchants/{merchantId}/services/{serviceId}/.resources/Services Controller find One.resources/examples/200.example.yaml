$kind: http-example
request:
  url: "{{baseUrl}}/merchants/:merchantId/services/:serviceId"
  method: GET
  pathVariables:
    merchantId: string
    serviceId: string
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
