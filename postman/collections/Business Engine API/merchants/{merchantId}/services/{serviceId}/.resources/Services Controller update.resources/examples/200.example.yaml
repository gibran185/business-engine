$kind: http-example
request:
  url: "{{baseUrl}}/merchants/:merchantId/services/:serviceId"
  method: PATCH
  headers:
    Content-Type: application/json
  pathVariables:
    merchantId: string
    serviceId: string
  body:
    type: json
    content: "{}"
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
