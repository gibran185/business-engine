$kind: http-example
request:
  url: "{{baseUrl}}/merchants/:merchantId/staff/:staffId"
  method: PATCH
  headers:
    Content-Type: application/json
  pathVariables:
    merchantId: string
    staffId: string
  body:
    type: json
    content: "{}"
response:
  statusCode: 200
  statusText: OK
  body:
    type: text
    content: ""
order: 1000
