$kind: http-example
request:
  url: "{{baseUrl}}/merchants/:merchantId/appointments"
  method: POST
  headers:
    Content-Type: application/json
  pathVariables:
    merchantId: string
  body:
    type: json
    content: "{}"
response:
  statusCode: 201
  statusText: Created
  body:
    type: text
    content: ""
order: 1000
